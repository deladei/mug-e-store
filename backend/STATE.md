# STATE

Single source of "where we are" and "what's next". Read this at session start.

## Current status

- **Session:** 8 — ✅ complete (2026-06-19). Closed the loose end S7 had predicted: S7's trailing `docs(state)` commit was never landed (STATE + SESSION_LOG sat uncommitted in the tree). Committed it as `71484a0` `docs(state)` + pushed `origin/main`; mirrored same-session to the monorepo — replaced `backend/` with standalone HEAD via `git archive` (only the 2 docs files changed, **0** `frontend/` touched), commit on `backend-bootstrap` → **refreshed open PR #2** (`backend/` verified byte-identical to standalone HEAD). No code change; build/test untouched and green from S7. Backend is feature-complete (Phase 1 + Phase 2 stretch + deploy/security tooling); the only outstanding work is the two owner-only items (Paystack live-E2E §4, deploy execution §6) — not doable from this sandbox.
- **Session:** 7 — ✅ complete (2026-06-19). Picked up an interrupted prior session that had left the **CI-gated deploy-on-merge** work staged-only in the working tree (`.github/workflows/deploy.yml`, `render.yaml` `autoDeploy:false`, README + DECISIONS + SESSION_LOG). Reviewed the diff (coherent, no Go change), ran build/vet/test (green; DB suites skip w/o `TEST_DATABASE_URL`), committed `867ddf3` `feat(deploy)` + pushed `origin/main`. Mirrored to monorepo: standalone HEAD copied byte-identical into `backend/` (76/76 files, **0** `frontend/` touched), commit `45db694` on `backend-bootstrap` → **updated open PR #2**. Wired `gh auth setup-git` (sandbox had no git credential helper; gh logged in as `deladei` with `workflow` scope). The two owner-only items (Paystack live-E2E, deploy execution) remain the only outstanding work.
- **Session:** 6 — ✅ complete (2026-06-18). (1) **Finished S4 delivery** — S5 had left it committed-only in the working tree; reviewed the diff (fixed one stale doc comment), ran build/vet/`test -p 1` (green), committed `a0feb77` + pushed `origin/main`; mirrored to monorepo `344fc57` and opened **PR #2** (PR #1 was already merged, so a new PR was required — see Pushed-to-remotes). (2) **Added the dev run/seed tooling** the plan §2 assumed but that never existed: `cmd/seed` (idempotent, `DATABASE_URL`-only, via `internal/store`) loads an 8-item / 14-variant Ghanaian Accra menu + `admin@coffeemug.shop`/`staff@coffeemug.shop` (`password123`); a `Makefile` (build/run/seed/test/vet/migrate-up/migrate-down); a real README. Proven end-to-end on a throwaway DB (migrate-up → seed 4/8/14 + 2 staff → re-run is a clean no-op). The **only** work left is the two owner-only items (Paystack live-E2E §4, deploy §6). Decision recorded.
- **Session:** 5 — ✅ complete (2026-06-16). Shipped Phase 2 **S4 guest checkout** (backend). New `POST /api/v1/auth/guest {name?, phone?}` (rate-limited) mints a **passwordless guest** — a real `users` row with `is_guest=true` (migration `0003`), a synthetic unique non-routable email, and an unusable bcrypt hash — and returns the standard login session, so cart/checkout/ownership/SSE/history all work unchanged. Guests earn no loyalty (`TransitionOrder` skips the earn row when `is_guest`). `internal/store` plumbs `User.IsGuest` through CreateUser/Get*/scanUser; `handleGuestSession` added. Tests added at both layers (round-trip, guest-earns-0, session usable, anonymous allowed, **guest cannot log in → 401**). `go build`/`vet` clean; full `go test -p 1 ./...` green with `TEST_DATABASE_URL`. Published to API brief §2.4; decision recorded. Chose the passwordless-user model over a nullable `orders.user_id` (zero changes to user_id-keyed paths). **Public-API-shape change (CLAUDE.md §9)** — purely additive; the in-progress working tree carried the design (handler comments + migration comment), so this session finished, tested, documented, and shipped it.
- **Session:** 4 — ✅ complete (2026-06-15). Verified the monorepo mirror: PR #1 was already current (a prior pass mirrored standalone `7727016` as monorepo `b42014c`), so the "PR behind" item was stale and is now corrected throughout this file — no re-mirror needed. No backend code changed. Next substantive work is still owner-gated (Paystack E2E / deploy) or the S4 guest-checkout API-shape decision; see Next action.
- **Session:** 3 — ✅ complete (2026-06-14). This session: shipped Phase 2 **S3 password reset** (backend-only — request + confirm endpoints, hashed single-use 1h tokens, session-revocation-on-reset, no enumeration), migration `0002`, store + HTTP tests. Working tree clean.
- **Session 2:** ✅ complete (2026-06-13). Finished the httpapi webhook, added `cmd/api`, shipped loyalty redemption, wrote the full DB-backed + HTTP test suites, kept the monorepo PR mirrored.
- **Runnable:** ✅ `cmd/api` binary builds and boots; fails fast with an aggregated config error when required env is unset.
- **Loyalty redemption:** ✅ feature complete + DB-tested. Pure `domain.Redemption` rule (9 unit cases); `store.Checkout` redeems inside the txn under a user-row `FOR UPDATE` lock; `TransitionOrder` refunds on cancel. Contract published to the API brief.
- **Build:** `go build ./...` clean; `go vet ./...` clean.
- **Tests:** `go test ./...` green; **with `TEST_DATABASE_URL` set, `go test -p 1 ./...` green** (incl. DB-backed store + HTTP-level; password-reset store + HTTP suites added Session 3). Without the var the store/httpapi DB suites skip, so default `go test ./...` stays green on a box without Postgres. **NB: Session 3 added migration `0002` — a reset of the test box needs both `0001` and `0002` applied (see Test DB below).**
- **Test DB:** local Postgres 18 on socket; created `coffeemug_test` (owner `walker`) with `0001_init`, `0002_password_reset_tokens` **and `0003_users_is_guest`** applied. DSN used: `host=/var/run/postgresql user=walker dbname=coffeemug_test sslmode=disable` (peer auth — TCP needs a password, the socket does not). To rebuild: `createdb coffeemug_test` then apply all three migration up files in order (`0001`, `0002`, `0003`). NB: Go lives at `/usr/local/go/bin` (not on PATH by default); export it and `GOTOOLCHAIN=local`.
- **Phase 0 (scaffolding):** ✅ committed `e83a81c`.
- **Phase 1 step 1 (schema migration):** ✅ committed `129defd`. Verified against Postgres (11 tables up, 0 down, re-up clean).
- **Phase 1 step 2 (domain state machine):** ✅ committed `890e349`. Exhaustive table-driven tests green (`internal/domain`).
- **Phase 1 step 3 (config loader):** ✅ committed `f48982f`. `internal/config` — env-only, refuses to boot without required secrets; tests green.
- **Phase 1 step 4 (auth):** ✅ committed `351c75e`. bcrypt cost 12, HS256 JWT (alg=none rejected), SHA-256 refresh hashing. Deps pinned for Go 1.22.
- **Phase 1 step 5 (store):** ✅ `internal/store` — `store.go` (handle, sentinels, users, refresh tokens), `catalog.go`, `cart.go`, `orders.go` (checkout txn with line snapshotting + cart clear; `TransitionOrder` with `FOR UPDATE` row lock → `domain.CanTransition` → audit event → loyalty earn on completed; ownership returns 404 not 403; idempotency-key NULL-on-empty). Builds + vets clean; **DB-backed tests deferred to Session 4** per plan.
- **Phase 1 step 6 (paystack):** ✅ `internal/paystack` — `Initialize`/`Verify` client (amount in pesewas, currency GHS, baseURL overridable) + `VerifySignature` (constant-time HMAC-SHA512). Tests green: signature valid/forged/wrong-secret/tampered/non-hex, plus Initialize/Verify via httptest.
- **Phase 1 step 7 (sse):** ✅ `internal/sse` — in-process per-order pub/sub broker; non-blocking publish, idempotent leak-free unsubscribe. Passes `-race`.
- **Phase 1 step 8 (httpapi):** ✅ committed `4f75b17`. `internal/httpapi` (10 files) — `server.go` route table + global chain (panic recovery → logging → CORS), `middleware.go` (Bearer/`?token=` auth, role gate, per-IP auth rate limit), handlers for auth/catalog/cart/checkout/orders/SSE/staff/admin, and `webhook_handlers.go` — the Paystack webhook, the **only** path to `paid`, enforcing TRD §5.2's four gates in order (signature → server-side verify → exact amount+GHS → legal transition), idempotent (paid→paid no-op), 5xx→retry / 200→stop, nil system actor. Standard error envelope throughout. The completing piece this session was the webhook handler (prior session stopped mid-step with it the one undefined symbol).
- **Phase 1 step 9 (cmd/api):** ✅ committed `9b8e778`. `cmd/api/main.go` — composition + lifecycle only: `config.LoadFromEnv` → `store.Open` + startup ping (fail fast) → `auth.NewTokenManager`, `paystack.NewClient`, `sse.NewBroker` → `httpapi.NewServer` → serve `Handler()`. `http.Server` with slowloris read timeouts but **`WriteTimeout: 0`** (a positive write deadline would sever live SSE streams); SIGINT/SIGTERM → bounded graceful `Shutdown`. Binary boots + fails fast on missing env.
- **Phase 1 step 10 (tests):** ✅ committed `8124f79` (store) + `013e4bb` (httpapi). Store suite against real Postgres — checkout (happy/empty/unavailable/duplicate-key), redemption (exact balance / over-balance reject / **concurrent double-spend → exactly one commit, balance 0, passes `-race`**), transition (legal/illegal/no-op/earn/refund-on-cancel). HTTP suite via `httptest` + fake Paystack — auth flow, wrong-password 401, ownership 404, staff 403 on manual `paid`, and the webhook four-gate matrix (happy/bad-sig/verify-failed/amount-mismatch/idempotent). Both skip without `TEST_DATABASE_URL`.
- **Phase 2 S2 (admin reports):** ✅ committed `b2ce07e`. `GET /admin/reports/summary?days=` (admin-only) — orders/day + revenue/day over a continuous (gap-filled) window; revenue counts only confirmed orders (not pending/cancelled). `store.ReportSummary` + handler + store & HTTP tests. Published to API brief §4.1.
- **Phase 2 S3 (password reset):** ✅ this session. Migration `0002_password_reset_tokens` (hash-only, single-use, 1h expiry; up/down verified, re-up clean). `auth.GenerateOpaqueToken`/`HashToken` (refresh helpers now delegate). `store.CreatePasswordResetToken` + `store.ConsumePasswordReset` (one txn: validate under `FOR UPDATE` → set password → clear user's reset tokens → revoke all refresh tokens). Endpoints `POST /auth/password-reset/{request,confirm}` (rate-limited, no enumeration, `invalid_token` for unknown/used/expired). **No email dep**: request logs the link server-side (`TODO(deploy)`) and never returns the token. Store + HTTP test suites added (request-is-generic, confirm happy/bad/expired/short-password, session-revocation). Published to API brief §2.9; recorded in DECISIONS.md. New error code `invalid_token`.
- **Pushed to remotes:** Backend repo `origin/main` at `71484a0` (S8 `docs(state)` recording S7). Monorepo: standalone HEAD `71484a0` mirrored byte-identical into `backend/` by a `backend-bootstrap` commit, which **updates the still-open PR [#2](https://github.com/Manyle4/mug-e-store/pull/2)** (into `main`). Only the 2 docs files changed vs the prior mirror, 0 `frontend/` touched. **NB (corrected this session):** the old PR [#1](https://github.com/Manyle4/mug-e-store/pull/1) was already **MERGED** into monorepo `main` (the S2+S3 mirror `b42014c` is in `main`), so it could not carry new work — pushing to `backend-bootstrap` does not reopen a merged PR. Hence S4 rides a fresh PR #2. Verified `backend/` tree byte-identical to standalone HEAD (65 files, matching blob hashes), **0** `frontend/` files touched.

## Next action

**Backend code + tests for Phase 1 are complete.** What remains is owner-only (cannot be done from this sandbox):
1. **Real-Paystack E2E (plan §4):** put a Paystack **test** secret key in `.env`, tunnel the local server (e.g. `ngrok http 8080`), register the webhook URL, run a real checkout with a test card, and confirm the order flips to `paid` over the live webhook + the SSE stream updates. Also verify a *pending* (not-success) payment does **not** flip the order.
2. **Deploy (plan §6):** host backend + Postgres; set `FRONTEND_ORIGIN`; switch the refresh cookie to `SameSite=None; Secure` if cross-origin.

If continuing in-sandbox instead: of the Phase 2 stretch list, **S1 (loyalty), S2 (admin reports), S3 (password reset), and S4 (guest checkout) are all done** — the backend Phase 2 stretch list is now complete. **Dev run/seed tooling is also done (2026-06-18):** `make migrate-up && make seed && make run` works (seeder = `cmd/seed`, 8-item Ghanaian menu + `admin@coffeemug.shop`/`staff@coffeemug.shop` `password123`); README documents the full local loop. **Deploy is now containerized (2026-06-18):** a multi-stage `Dockerfile` + `docker-compose.yml` (db→migrate→seed→api) build a ~25 MB image, verified end-to-end in containers (migrate+seed+healthz+`/categories` all green). Deploy is reduced to: push the image to a host, set real secrets, run `make migrate-up` (or the `migrate` service) once. **`render.yaml` Blueprint now checked in (2026-06-18):** Render → New → Blueprint → this repo provisions Postgres + the Docker web service, wires `DATABASE_URL`, generates `JWT_SECRET`, auto-runs migrations via `preDeployCommand`; the operator only sets `PAYSTACK_SECRET_KEY` + `FRONTEND_ORIGIN` and clicks deploy (free-tier caveats noted in-file). **Security hardening done (2026-06-18):** `securityHeaders` middleware, CI (`.github/workflows/ci.yml` = build/vet/test + govulncheck + 40% coverage floor), Docker base pinned to a patched Go, and `docs/production-readiness.md` + `docs/architecture.md` triaging the full prod checklist (✅ done / 🧭 non-goal / 🛠 operator). govulncheck = 0 vulns in our code paths (the 33 stdlib hits are the old local go1.22.4 only; CI/Docker build on current 1.22.x). **The only remaining work is the two owner-only items above** (Paystack live-E2E §4 needs a real test key + tunnel; deploy §6 needs a chosen host + real secrets) — neither is doable from this sandbox. Anything beyond them is new scope (ask the owner). When starting any task: re-create the test DB if this box was reset (`createdb coffeemug_test` + apply **all three** `migrations/0001_init.up.sql`, `0002_password_reset_tokens.up.sql`, `0003_users_is_guest.up.sql`; DSN in the status block above), and re-mirror the monorepo PR at the end per the standing two-target rule.

**Two owner-only follow-ups (also from S3):** (1) wire a real email provider so the reset link is emailed instead of logged — replace the single `TODO(deploy)` log call in `handlePasswordResetRequest`; (2) the deploy step must run `0002` against prod Postgres.

**Monorepo delivery — PR #2 OPEN, awaiting frontend-team merge.** `Manyle4/mug-e-store` PR #2 (`backend-bootstrap` → `main`, commit `344fc57`) carries S4, mirrored to standalone `a0feb77`. Byte-identical `backend/` tree (65 files, matching blob hashes), 0 `frontend/` files touched. **PR #1 is merged/closed** — do not reuse it. **Standing two-target rule, corrected:** after each standalone commit that changes tracked files, re-mirror onto `backend-bootstrap`; if the open PR (#2) is still unmerged, the push updates it automatically; once it merges, the *next* mirror needs a brand-new PR (a merged PR will not pick up new commits). The trailing `docs(state)` commit that records this push will be mirrored next session.

## Notes / open items

- **Two push targets** (see `DECISIONS.md` 2026-06-13):
  - `origin` = `https://github.com/deladei/coffemug-shop-backend-` — Go code at repo root. Local `main` is based on its existing "Initial commit" (README placeholder preserved).
  - `Manyle4/mug-e-store` — shared monorepo; backend code under `backend/`, delivered by **PR into `main`**, never touching `frontend/`.
- Specs live in `docs/` (moved there from repo root in Session 1).
